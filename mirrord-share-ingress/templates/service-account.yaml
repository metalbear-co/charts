{{- if .Values.sa.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: mirrord-share-ingress
    {{- include "mirrord-share-ingress.labels" . | nindent 4 }}
  name: {{ .Values.sa.name }}
  namespace: {{ .Values.namespace }}
{{- end }}
