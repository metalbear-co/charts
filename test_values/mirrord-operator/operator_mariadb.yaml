operator:
  mariadbBranching: true
  mariadbBranchConfig:
    dbPod:
      image:
        registry: "mariadb"
      volume:
        name: "mariadb-beautiful-data"
        emptyDir:
          sizeLimit: "1Gi"
      labels: { "role": "database" }
      annotations: { "db.branch/owner-team": "platform" }
      resources:
        requests:
          cpu: "500m"
          memory: "512Mi"
        limits:
          cpu: "1"
          memory: "1Gi"

license:
  file:
    secret: mirrord-operator-license
    data:
      license.pem: "DOESN'TNEEDTOBOOTSOITCANBEINVALID"
