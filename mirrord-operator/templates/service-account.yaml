{{- if .Values.sa.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- if or .Values.sa.roleArn .Values.sa.gcpSa .Values.sa.azureClientId }}
  annotations:
    {{- if .Values.sa.roleArn }}
    eks.amazonaws.com/role-arn: {{ .Values.sa.roleArn }}
    {{- end }}
    {{- if .Values.sa.gcpSa }}
    iam.gke.io/gcp-service-account: {{ .Values.sa.gcpSa }}
    {{- end }}
    {{- if .Values.sa.azureClientId }}
    azure.workload.identity/client-id: {{ .Values.sa.azureClientId }}
    {{- end }}
  {{- end }}
  labels:
    app: mirrord-operator
    {{- include "mirrord-operator.labels" . | nindent 4 }}
  name: {{ .Values.sa.name }}
  namespace: {{ .Values.namespace }}
{{- end }}
{{- if and .Values.operator.multiClusterMember (not .Values.operator.multiClusterMemberIamGroup) (not .Values.operator.multiClusterMemberAzureGroup) }}
---
# ServiceAccount used by the PRIMARY operator to authenticate to this cluster.
# Not needed when using IAM or AKS Workload Identity auth.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mirrord-operator-envoy
  namespace: mirrord
{{- end }}
